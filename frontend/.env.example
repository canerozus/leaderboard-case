VITE_API_BASE_URL=/api/v1
# Dev-only: where the Vite /api proxy sends requests. Defaults to http://api:3000
# (the compose service name) inside the frontend dev container. Override to
# http://localhost:3000 if running `npm run dev` directly on the host.
VITE_PROXY_TARGET=http://api:3000
